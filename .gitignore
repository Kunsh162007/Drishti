# Python
__pycache__/
*.pyc
*.pyo






# Secrets — never commit (examples ARE committed)
.env
.env.production
.env.local

# Misc
*.log
.DS_Store
node_modules/
.cache/

# Local Catalyst CLI artifacts (regenerated by `catalyst init`; deploy recipe lives in catalyst/ + CATALYST_DEPLOY.md)
/.catalystrc
/catalyst.json
/app-config.json

# Large generated / local-only files (kept out of the repo)
demo_submission/
production/*.db
